# Agent skills

Project skills from [mattpocock/skills](https://github.com/mattpocock/skills), installed for Cursor.

Restore / update:

```bash
npx skills@latest experimental_install
# or refresh from upstream:
npx skills@latest add mattpocock/skills --skill '*' --agent cursor -y
```

Lockfile: `skills-lock.json` at repo root.
